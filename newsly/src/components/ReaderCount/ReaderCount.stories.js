import React from 'react';
import { ReaderCount } from '.';

export default {
    title: 'Components/ReaderCount',
    component: ReaderCount
}

const Template = args => <ReaderCount {...args} />

export const Default = Template.bind({});
